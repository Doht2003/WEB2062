// Arrays 
const arrays = ['Code', 'Eat', 'Code'];

// Sets
const sets = new Set(['Code', 'Eat', 'Code']);

// Objects
const objects = {
    task: 'Code',
    date: 'today',
    repeat: true,
};

// Maps 
const maps = new Map([
    ['task', 'Code'],
    ['date', 'today'],
    [false, 'Start coding!'],
]);